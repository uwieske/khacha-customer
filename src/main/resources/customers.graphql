schema {
    query: Query
}

type Query {
    allCustomers: [Customer]
    customer(id: String) : Customer
}

type Customer {
    custno: String
    email: String
    name: String
    startDate: String
}

